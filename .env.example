# Copia a .env (NO se commitea). Valores reales solo local/CF Pages.
# GA4: debe llevar prefijo PUBLIC_ para exponerse al cliente (Astro).
PUBLIC_GA_ID=

# Loops.so — captura de email (solo servidor, sin prefijo PUBLIC_). Se configura en CF Pages.
LOOPS_API_KEY=
